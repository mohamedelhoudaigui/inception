
volumes:
  mariadb-volume:
    driver: local
    driver_opts:
      type: none
      device: /Users/mel-houd/test/mariadb
      o: bind

  wordpress-volume:
    driver: local
    driver_opts:
      type: none
      device: /Users/mel-houd/test/wordpress
      o: bind

networks:
  inception:
    name: inception
    driver: bridge

services:
  nginx:
    container_name: nginx
    build:
      context: ./requirements/nginx
      dockerfile: dockerfile
    image: nginx:mel-houd
    ports:
      - "443:443"
    volumes:
      - wordpress-volume:/var/www/html
    depends_on:
      - wordpress
    networks:
      - inception
    restart: always

  mariadb:
    env_file:
      - .env
    container_name: mariadb
    build:
      context: ./requirements/mariadb
      dockerfile: dockerfile
    image: mariadb:mel-houd
    volumes:
      - mariadb-volume:/var/lib/mysql
    restart: on-failure
    networks:
      - inception


  wordpress:
    env_file:
      - .env
    container_name: wordpress
    build: ./requirements/wordpress
    image: wordpress:mel-houd
    volumes:
      - wordpress-volume:/var/www/html
    depends_on:
      - mariadb
    restart: on-failure
    networks:
      - inception

